/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/design-patterns-blog',
    images: {
        unoptimized: true
    }
}

module.exports = nextConfig
